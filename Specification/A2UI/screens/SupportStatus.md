Visual: A timeline or progress bar derived from the auditLog.

Logic: * If status == 'In-Review', display the SLA Countdown Timer (3/7/10 days).

If status == 'Blocked', display the blocker message from the Gatekeeper.

Constraint: No input fields allowed here (Read-Only).